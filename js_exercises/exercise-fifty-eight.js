// cake-baking process
let cupsOfSugarNeeded = 0;
let cupsAdded = 0;
do {
  cupsAdded++;
} while (cupsAdded < cupsOfSugarNeeded)
console.log(cupsAdded);
// Add students' names to the list of qualifiers
let namesOfStudents = ['Mahsa', 'Asma', 'Nagina'];
let addedStudents = 0;
let i = 0;
do {
  addedStudents = namesOfStudents[i]
  i++;
  console.log(addedStudents);
} while (addedStudents < namesOfStudents);

// Add some cups of water to the boiling rice

let cupsOfWater = 5;
let addedCups = 0;
do {
  addedCups++;
  console.log(addedCups);
} while (addedCups < cupsOfWater);
